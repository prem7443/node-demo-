name: Test PR Trigger

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test_trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm PR event
        run: echo "✅ PR trigger is working!"
