name: Test Trigger

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a test workflow to confirm triggering on PR events"
