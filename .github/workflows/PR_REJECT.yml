name: Test PR Trigger

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR trigger test successful for PR #${{ github.event.pull_request.number }} on event ${{ github.event.action }}"
