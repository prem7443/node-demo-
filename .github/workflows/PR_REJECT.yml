name: Debug PR Trigger

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Just echo
        run: echo "Pull request trigger is working!"
